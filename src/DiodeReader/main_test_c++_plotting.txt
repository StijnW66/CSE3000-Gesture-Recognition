#define WITHOUT_NUMPY

#include"SimpleSmoothFilter.h"
#include"matplotlibcpp.h"
#include<vector>


namespace plt = matplotlibcpp;
int main() {

    SimpleSmoothFilter sf;

    std::vector<float> signal = {1, 2, 5, 3, 4 , 3 , 4, 3, 5, 5, 7, 9, 2};

    plt::plot(signal);

    sf.SmoothSignal(signal.data(), 13, 1);

    plt::plot(signal);
    
    plt::show();
}